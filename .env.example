# Used at build time for sitemap.xml + robots.txt Sitemap line, and at runtime for canonical / Open Graph when set.
# Example (no trailing slash):
# VITE_SITE_URL=https://your-production-domain.ge

VITE_SITE_URL=
